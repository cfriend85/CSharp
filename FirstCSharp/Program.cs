// Importing libraries we need
using System;

// Name of our library
namespace FirstCSharp
{
    class Program
    {
    // Scope(static means its on avail in its class) Return-type(void means no return) Name Parameter
    // If i wanted to return a number void would become int and it would have a return statement which would NEED to be an integer
        static void Main(string[] args)
        {
            Console.WriteLine("Welcome to day one of C#!!!");
            Console.WriteLine("New language time!!!");
        }
    }
}
